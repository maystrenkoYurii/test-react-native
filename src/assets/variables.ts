import { PixelRatio } from 'react-native';

export const variables = {
  headerHeight: PixelRatio.roundToNearestPixel(56),
  listItemHeight: PixelRatio.roundToNearestPixel(86),
  optionHeight: PixelRatio.roundToNearestPixel(44),
  disabledOpacity: 0.6,
  activeOpacity: 0.5,
};
